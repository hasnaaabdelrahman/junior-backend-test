# Challenge 2 - Database Query Optimization


## Optimization
For high traffic:
- Add indexes on frequently searched fields like price and category.
- Use caching (for example Redis) for repeated queries.
- Use pagination to avoid loading too much data at once.
